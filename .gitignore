# Build artifacts
build/*.img
build/*.qcow2
build/*.vmdk
build/*.ova
build/*.ovf
!build/template.ovf
build/*.mf
build/cidata.iso
build/govc
build/http/
build/ignition/
build/packer/

# Local config and Secrets
config/*.env
!config/secrets.env
config/vault.yml
config/.vault_pass
!config/vault.yml.example
ansible/inventory.ini

# Generated by `make sync-ci-runners`; keep only directory docs/placeholders.
ansible/generated/ci_runner/*
!ansible/generated/ci_runner/.gitkeep
!ansible/generated/ci_runner/README.md

# Terraform/OpenTofu
*.tfstate
*.tfstate.backup
*.tfplan
crash.log
crash.*.log
.terraform/
.terraform.lock.hcl
tofu/workspaces/
tofu/.terraform/
tofu/.terraform.lock.hcl
tofu/terraform.tfstate*
tofu/terraform.tfstate.d/
tofu/dns/records.tf.json
tofu/dns/terraform.tfvars
.worktrees/
__pycache__/

__pycache__
*.pyc
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
coverage.json
htmlcov/

# Python virtualenv
venv./
venv/
.venv/

# Packer build artifacts (contain local passwords at build time)
packer/http/ubuntu2404/meta-data
packer/fcos-install.ign
packer/fcos-live.ign
packer/fcos-live.bu
packer/http/fcos/installed.bu

# 1Password mock binary (local dev only)
build/govc.mock

# DHCP migration state
.dhcp-migration-state.json
config/.env.local
config/technitium.token
config/top_*.txt
results/
benchmarks/results/
benchmarks/reports/
benchmarks/configs/
prefill*.log
homelab-gitops-python.cdx.json

# Tool-specific agent pointer; AGENTS.md is the canonical directive (Master-Policy 1.6)
CLAUDE.md
