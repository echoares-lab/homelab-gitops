# Agent Directives

**See [AGENTS.md](AGENTS.md)** — the canonical, vendor-neutral directive file for this
repository. It carries the mandatory policy, knowledge-authority protocol, toolchain
mandates, and documentation rules that apply to every agent (Gemini, Claude, AGY, Codex, Cursor).

Global authority: `dev-policies` (`/home/dev/repos/dev-policies/policy/rules/*.yaml`).
Project documentation authority: In-repo under `docs/`.
