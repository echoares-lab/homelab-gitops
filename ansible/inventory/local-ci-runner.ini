# Local self-hosted runner (no vSphere inventory). Use on the runner host itself:
#   make apply-ci-runners-local
#
[tag_git_test]
dev-01 ansible_connection=local

[tag_git_test:vars]
ansible_user=dev
runner_home=/home/github-runner
